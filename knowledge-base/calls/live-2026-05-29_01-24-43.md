# Live call assistant — 2026-05-29 01:24:43

_Remote party transcript with computed context/answers. `tail -f` this during the call._

### 01:26:10

## ⏩ research → clinic

**Q:** 'How can we actually make these products of research useful to clinicians when they do their medical practice?'

**Say:** The bottleneck isn't the algorithm — it's getting a research pipeline into the tools clinicians already use. ChRIS packages research as containerized, reproducible plugins that run the same in lab and clinic (KB 01/10). Gap-closer = interoperability: we put **DICOMweb (QIDO/WADO/STOW)** on ChRIS, so a standard viewer like **OHIF** can query/retrieve/store imaging straight out of ChRIS over the same web standard a hospital PACS speaks (KB 05/12). Research outputs flow into existing clinical tooling. Proven end-to-end against real ChRIS (KB 14).

_Broader 'AI useful to clinicians' also = regulatory/validation + workflow fit (general)._

### 01:28:16

## (same talk replaying)

Same 'Modernization of DICOM Workflows' talk as before; opener (research→clinic) already answered above. Now in DICOM-history setup (1985, 11y before HTTP, v3=TCP/IP).

**Arc heading toward (prep ready):** oxidicom (Rust receive/index) → Django-CPU-throttle → SQLX direct-to-Postgres. Your line: DICOMweb is the SERVING half on oxidicom; **WADO HTTP-GET pull fixes C-MOVE reversed-client-server**; **variant-C** consumes oxidicom tags, no re-read (KB 05/11/12). Nothing new directed at Marty.

### 01:39:57

## Q&A — parallelism (to the speaker)

Captured 'latest question' mis-pinned to the opener (already answered). Actual latest in FULL = parallelism Q to the speaker, who's answering.

**Answer (KB 11):** single multi-threaded process, **thread pool** (OXIDICOM_LISTENER_THREADS, one per concurrent association) — NOT multiple single-threaded executables. OS threads because **dicom-rs is sync-only**; other deps async on Tokio → sync-pool + async-pool hybrid. Plus **horizontal replicas** (floor cluster) → vertical + horizontal scaling.

Talk fully replayed + at Q&A; same arc as prior session. Nothing new for Marty; prepped lines stand (DICOMweb=serving half; WADO pull fixes C-MOVE; variant-C no re-read).

