# Live call assistant — 2026-05-29 00:48:04

_Remote party transcript with computed context/answers. `tail -f` this during the call._

### 00:49:43

## ⏩ Latest question — making research useful to clinicians

**Q (verbatim):** "…how can we actually make these products of research useful to clinicians when they do their medical practice?" (speaker: BCH scientist+software dev, brain-MRI lab)

**Say:** The bottleneck isn't the algorithm — it's getting a research pipeline into the tools/workflow clinicians already use. That's what ChRIS is for: research packaged as containerized, reproducible plugins/pipelines (KB 01, 10). The gap-closer is interoperability — clinicians live in PACS + standard viewers, so we added **DICOMweb (QIDO/WADO/STOW)** to ChRIS: a standard viewer like **OHIF** can query/retrieve/store imaging straight out of ChRIS over the same web standard a hospital PACS speaks (KB 05, 12, 08-D1). Research outputs flow into existing clinical tooling. And it's proven end-to-end against real ChRIS (KB 14), not slideware.

_Broader 'AI useful to clinicians' also = regulatory/validation + trust + workflow fit (general, not KB)._

**Situation:** opener; BCH brain-MRI scientist-dev, research→clinic. Matches engagement (KB 15).

### 00:51:34

## Talk in progress — 'Modernization of DICOM Workflows'

Captured latest question unchanged (research→clinic, answered prior). Remote party is now PRESENTING: DICOM = ubiquitous but 'legacy' protocol; framing its age vs HTTP 1.0 (1996) / JPEG (1992); teed up 'guess how old DICOM is.'

**Rhetorical hook answer (KB 04 lacks dates — general knowledge):** DICOM lineage = ACR-NEMA 1.0 in **1985** (2.0 1988), **DICOM 3.0 in 1993** — older than the web (HTTP 1.0 1996), ~as old as JPEG. That's the 'legacy' punchline.

**Bridge to our work (KB-grounded, ready):** the 'modernization' = **DICOMweb** (QIDO/WADO/STOW, application/dicom+json) — KB 05. Our spike put DICOMweb **on ChRIS** + proved it end-to-end (KB 12, 14). When the talk lands on 'how to modernize,' that's the working answer.

**Where we are:** still scene-setting toward the DICOMweb punchline; nothing directed at Marty yet.

### 00:55:26

## Talk reached OUR domain — oxidicom + DICOMweb

Latest captured question unchanged. Talk now: DICOM history (1985, 11y before HTTP, v3=TCP/IP), reversed client-server quirk; research flow (scan→DICOM on PACS→pull→pipeline e.g. brain segmentation); Python DICOM-receive perf pain (100s subprocesses, 1-5min/scan, maxed HPC); **'rewrote bottleneck in Rust — reads DICOM metadata, stores in index DB for search' = OXIDICOM** (KB 11).

**Connections ready (KB):**
- Their Rust indexer = oxidicom; our spike sits ON it: L2 **QIDO-RS serves that index over HTTP**, +WADO/STOW (KB 11, 12).
- 'Reversed client-server' = **DIMSE C-MOVE** (server pushes); **DICOMweb fixes it — WADO-RS is a normal HTTP GET pull** (KB 05). The concrete modernization payoff.
- Next step from 'index DB for search' = **variant C**: feed oxidicom's parsed tags straight into the DICOMweb index, NO re-read — prototyped + proven (KB 12, 14).

**Situation:** remote speaker is the oxidicom author (BCH); presenting the problem→Rust-indexer arc our spike extends. Be ready when they hit 'how do we serve this.'

### 00:59:52

## Their production arch (oxidicom→Postgres via SQLX) — next to our D1

Captured 'latest question' = a slide JOKE ('new hire uses Excel') — NOT a question; ignore.

**New substance:** Rust receiver 100-200x faster than Python (faster than `python -c ''`). Production problem: Rust **pushes to a Django server → Django CPU throttles → receive errors** (Grafana, mem spikes). Their fix ('heretical'): Rust **writes DICOM metadata straight to PostgreSQL, bypassing Django**, via **SQLX** crate which **compile-time-verifies** SQL vs schema (cargo build fails on typo) → safe tight coupling.

**Connection ready (KB):** their direct-to-Postgres ≈ **variant B** (push indexing into Rust, skip the Python re-read — KB 08 D1; why variant A re-read is weakest). Crucially: **our DICOMweb QIDO/WADO/STOW is indifferent to HOW the index is populated** — it reads CUBE's Postgres (PACSStudy/PACSInstance). So whether oxidicom writes directly (their SQLX path ≈ B), a NATS consumer does (our variant-C, no re-read — KB 12), or Django re-reads (Phase A), the same web API serves it. Watch-item: SQLX direct-write = schema coupling to CUBE migrations (SQLX catches at compile; tradeoff vs message-decoupled).

**Situation:** oxidicom author showed ingest/index hardening; next beat = SERVING it = our QIDO/WADO/STOW.

### 01:03:05

## Talk ENDED ('Thank you' 01:02:17) — your moment / Q&A

Captured 'latest question' still the Excel JOKE — not a question. No real Q put to Marty.

**Talk close:** Rust lessons — SQLX correctness, no-GC predictable memory, low cloud cost + commodity hardware; difficulties — fragmented Rust ecosystem (sync-vs-async: DICOM libs sync, OpenTelemetry async), Rust unpopular in Python/MATLAB domain. Punchline: their DICOM cluster = 10-15yo office desktops (some 4GB) + Kubernetes, no server farm. Host = 'Matthew'.

**Marty's move (KB):** talk covered the INGEST/INDEX half (oxidicom). The complement clinicians need is SERVING it modern-ly = our **DICOMweb QIDO/WADO/STOW on ChRIS** (KB 12, proven KB 14). Direct fix for the 'reversed client-server' pain: **WADO = plain HTTP GET pull, no C-MOVE** (KB 05). Fits their efficiency ethos (serving is light). Tie-in to their SQLX direct-Postgres: **variant C** = oxidicom publishes parsed tags → small consumer indexes, **no Django re-read** (KB 11,12) — exactly the Django-CPU-throttle failure on their Grafana.

**Situation:** post-talk gap, likely Q&A. Be ready to volunteer the serving-layer + decoupled-indexing complement.

### 01:04:10

## Q&A started — parallelism Q (to the speaker)

**Q (verbatim, audience→speaker):** 'how do you do parallelism? multi-threaded image or multiple single-threaded executables or something else?' Speaker is answering (sync/async, dicom-rs).

**Answer (KB 11):** Single multi-threaded process w/ a **thread pool** — NOT multiple single-threaded executables. `OXIDICOM_LISTENER_THREADS` worker threads, each = one concurrent DICOM association (default 8; 32 in the ChRIS dev stack). Uses OS threads not async because **dicom-rs is synchronous** (the sync/async divide the speaker cited). Parallel association threads feed a **single non-async state-loop consumer** that turns instances into events.

**Situation:** post-talk Q&A, host opened floor (typed Qs). This Q is to the speaker, not Marty. If Marty gets a Q it'll likely be serving/clinical-consumption side → DICOMweb-on-ChRIS answer ready (prior cards).

### 01:05:09

## Speaker answered the parallelism Q (confirms KB 11)

Captured Q unchanged (parallelism); speaker answered it live: dicom-rs is **sync-only → must use threads** (= our OXIDICOM_LISTENER_THREADS thread pool, KB 11 ✔); other deps async-only on **Tokio**, so a messy **async pool + sync pool** coexist (the sync/async divide). Plus **horizontal scaling via replicas** (floor cluster) → both vertical + horizontal.

Nothing for Marty. DICOMweb-on-ChRIS / variant-C answer stays queued for any serving-side Q.

